Project documentation lives here.

## Manual downloads

- These PDFs are committed to the repo and are the sole source of truth for generated data.
- Do not fetch third-party mirrors.

## Tooling

- The repo uses pnpm. Install dependencies with `pnpm install` and run scripts via `pnpm <script>`.
